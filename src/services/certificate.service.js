import fs from 'fs';import path from 'path';import PDFDocument from 'pdfkit';
export async function generatePdf(certificate,user,course){const dir='uploads/certificates';fs.mkdirSync(dir,{recursive:true});const file=path.join(dir,`${certificate.certificateNumber}.pdf`);const doc=new PDFDocument();doc.pipe(fs.createWriteStream(file));doc.fontSize(26).text('TeacherPoint Certificate',{align:'center'});doc.moveDown().fontSize(18).text(`Awarded to ${user.name}`,{align:'center'});doc.text(`For completing ${course.title}`,{align:'center'});doc.text(certificate.certificateNumber,{align:'center'});doc.end();return file;}
