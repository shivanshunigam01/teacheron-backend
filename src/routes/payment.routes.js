import {Router} from 'express';import * as c from '../controllers/payment.controller.js';import {verifyJWT} from '../middleware/auth.middleware.js';const r=Router();r.post('/',verifyJWT,c.create);r.get('/me',verifyJWT,c.mine);r.get('/:id',verifyJWT,c.getById);r.post('/:id/unlock-contact',verifyJWT,c.unlock);export default r;
