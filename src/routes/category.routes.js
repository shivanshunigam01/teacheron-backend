import {Router} from 'express';import * as c from '../controllers/category.controller.js';import {verifyJWT,requireRole} from '../middleware/auth.middleware.js';const r=Router();
r.get('/',c.list);r.post('/',verifyJWT,requireRole('admin'),c.create);r.get('/:id',c.getById);r.patch('/:id',verifyJWT,requireRole('admin'),c.update);r.delete('/:id',verifyJWT,requireRole('admin'),c.remove);export default r;
