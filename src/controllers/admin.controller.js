import User from '../models/User.model.js';import Course from '../models/Course.model.js';import Accommodation from '../models/Accommodation.model.js';import Payment from '../models/Payment.model.js';import AdminMember from '../models/AdminMember.model.js';import Report from '../models/Report.model.js';import SmtpConfig from '../models/SmtpConfig.model.js';import {ApiResponse} from '../utils/ApiResponse.js';import {asyncHandler} from '../utils/asyncHandler.js';import {toJSON,toJSONList} from '../utils/serialize.js';
export const stats=asyncHandler(async(req,res)=>{const [courses,teachers,accommodations,revenue]=await Promise.all([Course.countDocuments(),User.countDocuments({role:'teacher'}),Accommodation.countDocuments(),Payment.aggregate([{$match:{status:'paid'}},{$group:{_id:null,total:{$sum:'$amount'}}}])]);ApiResponse.ok(res,{courses,teachers,packages:0,accommodations,inquiriesNew:0,revenueMtd:revenue[0]?.total||0,enrollmentsWeek:[]},'Stats fetched');});
export const team=asyncHandler(async(req,res)=>ApiResponse.ok(res,toJSONList(await AdminMember.find().populate('userId')),'Team fetched'));
export const invite=asyncHandler(async(req,res)=>{const item=await AdminMember.create({userId:req.body.userId,staffRole:req.body.staffRole,invitedBy:req.user.id});ApiResponse.created(res,toJSON(item),'Admin invited');});
export const reports=asyncHandler(async(req,res)=>ApiResponse.ok(res,toJSONList(await Report.find().sort('-createdAt')),'Reports fetched'));
export const updateReport=asyncHandler(async(req,res)=>ApiResponse.ok(res,toJSON(await Report.findByIdAndUpdate(req.params.id,{...req.body,resolvedBy:req.user.id},{new:true})),'Report updated'));
export const getSmtp=asyncHandler(async(req,res)=>ApiResponse.ok(res,toJSON(await SmtpConfig.findOne({isActive:true})),'SMTP fetched'));
export const updateSmtp=asyncHandler(async(req,res)=>ApiResponse.ok(res,toJSON(await SmtpConfig.findOneAndUpdate({isActive:true},{...req.body,isActive:true},{upsert:true,new:true})),'SMTP updated'));
export const broadcast=asyncHandler(async(req,res)=>ApiResponse.ok(res,{sent:true},'Broadcast queued'));
