import Category from '../models/Category.model.js';import {ApiError} from '../utils/ApiError.js';import {ApiResponse} from '../utils/ApiResponse.js';import {asyncHandler} from '../utils/asyncHandler.js';import {getPagination,paginationMeta} from '../utils/pagination.js';import {toJSON,toJSONList} from '../utils/serialize.js';
export const list=asyncHandler(async(req,res)=>{const {page,limit,skip,sort}=getPagination(req.query);const filter={};if(req.query.q) filter.$text={$search:req.query.q}; for(const k of ['status','category','city','country','role','targetType']) if(req.query[k]) filter[k]=req.query[k]; const [items,total]=await Promise.all([Category.find(filter).sort(sort).skip(skip).limit(limit),Category.countDocuments(filter)]);ApiResponse.ok(res,{items:toJSONList(items),pagination:paginationMeta(total,page,limit)},'Fetched successfully');});
export const getById=asyncHandler(async(req,res)=>{const item=await Category.findById(req.params.id);if(!item) throw ApiError.notFound();ApiResponse.ok(res,toJSON(item),'Fetched successfully');});
export const create=asyncHandler(async(req,res)=>{const item=await Category.create({...req.body,createdBy:req.user?.id,sellerId:req.user?.id,userId:req.user?.id});ApiResponse.created(res,toJSON(item),'Created successfully');});
export const update=asyncHandler(async(req,res)=>{const item=await Category.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true});if(!item) throw ApiError.notFound();ApiResponse.ok(res,toJSON(item),'Updated successfully');});
export const remove=asyncHandler(async(req,res)=>{const item=await Category.findByIdAndDelete(req.params.id);if(!item) throw ApiError.notFound();ApiResponse.ok(res,{},'Deleted successfully');});
